package com.basics.java.fundamentals.collections;

/*
 * Map:
 * - Group of objects are represented using key value pair
 * - Duplicate keys are not allowed but values can be duplicated
 * - Map is not a child interface of collection
 * - HashMap, LinkedHashMap , WeakHashMap, IdentityHashMap, Hashtable(Dictionary) , Properties are the implementation classes for map interface.
 * 
 * SortedMap:
 * - Group of objects are represented using key value pair according to sort order of keys
 * - Child interface of map
 * 
 * NavigableMap:
 * - Child interface of sortedMap
 * - Several utility methods for navigation are defined 
 * - Implementation class is TreeMap
 * */
public class CollectionsMap {
    public static void main(String[] args) {

    }
}
